# Shared Contracts

Generated types from `schemas/` should live here.

- `packages/shared/ts/`: TypeScript interfaces/types
- `packages/shared/python/`: Python typed models/stubs

Current source of truth is the JSON schema set in `schemas/`.
